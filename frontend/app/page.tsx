export default function Home() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-gray-500">DLOA Pages — acesse <code>/cliente-a</code> ou <code>/cliente-b</code></p>
    </div>
  );
}
